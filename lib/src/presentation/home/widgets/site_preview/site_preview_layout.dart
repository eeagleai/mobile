/// Spacing between the site preview header block and action buttons.
const sitePreviewHeaderToActionsGap = 20.0;

/// Spacing between the drag handle and header content.
const sitePreviewHandleToHeaderGap = 16.0;
